{
  "name": "srt-fixer",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "bin": {
    "srt-fixer": "index.ts"
  },
  "scripts": {
    "build": "bun build --compile index.ts --outfile dist/srt-fixer"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
